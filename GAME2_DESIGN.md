# 2번 게임 설계서 — 숨은 명대사 맞히기

## 1. 목적과 전제

- 기존 Next.js App Router, Supabase, Vercel 구성과 `DO / GAE / GEOL / YUT / MO` 5개 팀을 그대로 사용한다.
- 행사 진행자가 한 화면을 빔프로젝터에 띄우고 직접 조작하는 1회성 게임이다.
- 참가자는 별도 기기로 접속하지 않는다. 게임 화면과 점수 변경은 관리자 로그인 후에만 가능하게 한다.
- 새 서비스나 별도 Supabase 프로젝트는 만들지 않고 현재 앱에 `/admin/game2` 경로로 추가한다.
- 팀 아이콘은 기존 파일을 재사용한다.

| 팀 | 동물 | 기존 아이콘 |
|---|---|---|
| 도 (`DO`) | 돼지 | `/team-icons/pig.png` |
| 개 (`GAE`) | 강아지 | `/team-icons/dog.png` |
| 걸 (`GEOL`) | 양 | `/team-icons/sheep.png` |
| 윷 (`YUT`) | 소 | `/team-icons/cow.png` |
| 모 (`MO`) | 말 | `/team-icons/horse.png` |

## 2. 게임 규칙

1. 한 라운드에 문장 하나를 띄운다.
2. 최초에는 글자를 모두 가린다. 띄어쓰기와 문장부호는 보이게 하여 문장 구조를 알 수 있게 한다.
3. 진행자가 가려진 글자 칸을 누르면 그 글자만 공개된다. 이 공개 자체에는 점수가 없다.
4. 참가 팀이 공개된 글자를 맞히면 해당 팀에 1점을 준다.
5. 참가 팀이 전체 문장을 맞히면 해당 팀에 3점을 준다.
6. `전체 문장 공개`는 진행자가 정답 확인 또는 라운드 종료에 사용하는 기능이며 그 자체로 점수를 주지 않는다.
7. 모든 라운드가 끝나면 누적 점수가 가장 높은 팀이 승리한다. 동점은 공동 우승으로 표시한다.

### 판정 원칙

- “글자 정답”의 범위는 진행자가 현장에서 판정한다. 앱은 음성 인식이나 자동 채점을 하지 않는다.
- 한 팀이 같은 글자를 여러 번 말해도 한 번만 득점시키는 운영을 권장한다.
- 전체 문장 정답 3점은 해당 라운드에서 한 번만 부여하는 것을 기본값으로 한다.
- 진행자가 힌트로 글자를 공개했거나 아무도 못 맞혀 전체 문장을 공개한 경우에는 점수를 주지 않는다.

## 3. 권장 점수 UX

### 결론: 공개와 채점을 분리한다

`팀 아이콘 선택 → 글자 칸 클릭 시 +1`, `팀 아이콘 선택 → 전체 공개 클릭 시 +3` 방식은 빠르지만 다음 문제가 있다.

- 힌트를 주려고 글자를 눌렀는데 팀이 선택된 채라면 의도치 않게 점수가 올라간다.
- 전체 공개는 “아무도 못 맞혀 정답을 보여주는 경우”에도 필요한데 자동으로 3점이 붙을 수 있다.
- 이미 공개된 글자를 다시 눌렀을 때 중복 득점 여부가 모호하다.
- 프로젝터를 보며 진행할 때 현재가 공개 모드인지 채점 모드인지 놓치기 쉽다.

따라서 화면 하단의 고정 채점 바를 사용한다.

1. 정답을 말한 팀의 동물 아이콘을 누른다.
2. 바로 옆의 `글자 정답 +1` 또는 `문장 정답 +3`을 누른다.
3. 화면에 `개 팀 +1` 같은 짧은 확인 효과를 표시한다.
4. 마지막 채점은 `실행 취소`로 되돌릴 수 있다.
5. 점수를 준 뒤에는 팀 선택을 자동 해제한다. 연속 오입력을 막기 위함이다.

글자 칸과 `전체 문장 공개` 버튼은 팀 선택 여부와 무관하게 항상 공개 기능만 수행한다. 따라서 너무 어려울 때 진행자가 자유롭게 한 글자씩 힌트를 줄 수 있다.

### 빠른 진행을 위한 보조 기능

- 키보드 `1~5`: 도, 개, 걸, 윷, 모 팀 선택
- 키보드 `Q`: 선택 팀 +1
- 키보드 `W`: 선택 팀 +3
- 키보드 `Z`: 마지막 점수 실행 취소
- 키보드 `Space`: 다음 라운드 확인창 열기
- 단축키는 입력 칸이 없을 때만 동작하고, 화면 하단에 작게 안내한다.

## 4. 화면 설계

### 상단 고정 점수판

- 다섯 팀을 한 줄로 표시한다: 동물 아이콘, 팀 이름, 누적 점수.
- 선택된 팀은 굵은 테두리와 배경색으로 확실히 구분한다.
- 선두 팀에는 왕관 아이콘을 표시할 수 있으나, 공동 선두는 모두 표시한다.
- 작은 노트북과 프로젝터 모두 고려하여 5개 카드가 최소 1024px 너비에서 한 줄로 보여야 한다.

### 중앙 게임 영역

- 상단: `3 / 12` 형태의 라운드 진행률과 분류(`속담`, `영화`, `드라마`).
- 중앙: 큰 글자 칸으로 숨은 문장 표시.
- 한글 음절, 숫자, 영문은 각각 클릭 가능한 한 칸으로 만든다.
- 띄어쓰기는 넓은 간격으로, 따옴표·마침표·물음표·느낌표는 처음부터 표시한다.
- 이미 공개된 칸은 다시 눌러도 상태가 바뀌지 않는다.
- 출처는 정답 누설 가능성이 있으므로 처음에는 숨기고, 전체 문장 공개 후 함께 표시한다.

### 중앙 하단 진행 버튼

- `전체 문장 공개`: 확인창 없이 공개하되 5초 동안 `공개 취소` 가능하게 한다.
- `다음 문장`: “현재 라운드를 종료할까요?” 확인 후 이동한다.
- `이전 문장`: 운영 실수 복구용. 기존 공개 상태는 유지한다.
- `게임 초기화`: 관리자 메뉴 안에 두고 2단계 확인을 거친다.

### 하단 고정 채점 바

- 다섯 팀 아이콘 선택 영역.
- `글자 정답 +1`, `문장 정답 +3`, `마지막 점수 취소` 버튼.
- 팀을 선택하지 않으면 점수 버튼은 비활성화한다.
- `+3`을 준 라운드에서는 다시 `+3`을 누를 경우 확인창을 띄운다.

### 종료 화면

- 최종 순위와 다섯 팀 점수를 큰 글씨로 표시한다.
- `점수 기록 보기`, `마지막 채점 취소`, `처음부터 다시 시작`을 제공한다.

## 5. 문장 데이터

문장은 코드에 고정된 `lib/game2/phrases.ts`로 둔다. 1회성 행사이므로 문장 편집용 관리자 CRUD는 만들지 않는 편이 낫다.

```ts
type Phrase = {
  id: string;
  text: string;
  source: string;
  category: "속담" | "영화" | "드라마";
};
```

등록 대상은 다음 12개다.

1. 가는 말이 고와야 오는 말이 곱다. — 속담
2. 낮말은 새가 듣고 밤말은 쥐가 듣는다. — 속담
3. 세 살 버릇 여든까지 간다. — 속담
4. 소 잃고 외양간 고친다. — 속담
5. 배보다 배꼽이 더 크다. — 속담
6. 묻고 더블로 가! — 《타짜》
7. 내가 왕이 될 상인가? — 《관상》
8. 아직 한 발 남았다. — 《아저씨》
9. 호의가 계속되면 그게 권리인 줄 알아요. — 《부당거래》
10. 느그 서장 남천동 살제? — 《범죄와의 전쟁》
11. 나랑 밥 먹을래? 나랑 죽을래? — 《미안하다, 사랑한다》
12. 누가 기침 소리를 내었는가? — 《태조 왕건》

11번 문장의 출처는 《미안하다, 사랑한다》로 확정했다. 문장 표기는 사용자가 제공한 문구를 기준으로 하되 행사 전에 오탈자와 실제 대사를 최종 검수한다.

## 6. 상태 모델

브라우저 상태와 Supabase 영구 상태를 다음처럼 구분한다.

- 브라우저 상태: 선택된 팀, 전체 공개 취소 타이머, 애니메이션 표시 여부.
- Supabase 상태: 현재 라운드, 라운드별 공개 글자 위치, 전체 공개 여부, 팀별 총점, 점수 변경 이력, 게임 종료 여부.

페이지를 새로고침하거나 Vercel 인스턴스가 바뀌어도 게임을 이어가야 하므로 핵심 상태는 Supabase에 저장한다. 모든 상태를 브라우저에만 두는 구현은 더 간단하지만 행사 중 새로고침 한 번으로 점수가 사라질 수 있어 권장하지 않는다.

## 7. Supabase 설계

기존 `animal_team` enum을 그대로 재사용한다.

### `game2_sessions`

| 컬럼 | 형식 | 설명 |
|---|---|---|
| `id` | uuid PK | 게임 세션 ID |
| `status` | text | `ready`, `playing`, `finished` |
| `current_round` | integer | 0부터 시작 |
| `scores` | jsonb | `{ "DO": 0, ... }` |
| `revealed` | jsonb | 문장 ID별 공개된 글자 인덱스 배열 |
| `fully_revealed` | jsonb | 전체 공개된 문장 ID 배열 |
| `created_at` | timestamptz | 생성 시각 |
| `updated_at` | timestamptz | 마지막 변경 시각 |

### `game2_score_events`

| 컬럼 | 형식 | 설명 |
|---|---|---|
| `id` | uuid PK | 이벤트 ID |
| `session_id` | uuid FK | 게임 세션 |
| `team` | animal_team | 득점 팀 |
| `points` | smallint | `1` 또는 `3`, 취소 기록은 `-1` 또는 `-3` |
| `reason` | text | `letter`, `phrase`, `undo` |
| `round_index` | integer | 득점한 라운드 |
| `reverts_event_id` | uuid nullable | 취소 대상 이벤트 |
| `created_at` | timestamptz | 기록 시각 |

RLS는 기존 테이블과 동일하게 활성화하고 공개 정책을 만들지 않는다. 브라우저가 Supabase를 직접 수정하지 않고, 관리자 쿠키를 검증한 Next.js API만 `SUPABASE_SECRET_KEY`로 접근한다.

점수는 `scores`에 합계를 보관하면서 `game2_score_events`에도 이력을 남긴다. 합계는 빠른 화면 표시용이고 이벤트는 오입력 취소 및 사후 확인용이다. 점수 변경은 가능하면 Supabase RPC 하나에서 합계 수정과 이벤트 추가를 트랜잭션으로 처리하여 중복 클릭과 부분 저장을 방지한다.

## 8. API 설계

모든 API는 기존 `ADMIN_SESSION_COOKIE`와 `isValidAdminSession()`으로 인증한다.

| 메서드/경로 | 역할 |
|---|---|
| `GET /api/admin/game2` | 현재 세션과 점수/공개 상태 조회 |
| `POST /api/admin/game2/session` | 새 게임 시작 또는 전체 초기화 |
| `PATCH /api/admin/game2/round` | 현재/이전/다음 라운드 이동 |
| `POST /api/admin/game2/reveal` | 특정 글자 공개 또는 전체 공개 |
| `POST /api/admin/game2/score` | 선택 팀에 1점 또는 3점 부여 |
| `POST /api/admin/game2/score/undo` | 마지막 미취소 점수 이벤트 되돌리기 |

각 변경 요청에는 현재 `updated_at` 또는 버전 번호를 함께 보내 낡은 화면의 중복 변경을 거부한다. 버튼은 요청 중 비활성화하며 서버 응답 성공 후 화면 상태를 확정한다.

## 9. 파일 구성

```text
app/
  admin/game2/page.tsx             관리자 인증 및 초기 데이터 로드
  admin/game2/game-board.tsx       실제 게임 UI와 키보드 조작
  api/admin/game2/...              세션/라운드/공개/점수 API
lib/
  game2/phrases.ts                 고정 문장 데이터
  game2/types.ts                   게임 상태 타입
  game2/validation.ts              팀, 점수, 인덱스 검증
supabase/migrations/
  0003_game2.sql                   테이블, 인덱스, RLS, RPC
```

기존 `TEAM_CODES`, `TeamCode`, `TEAM_INFO`, 관리자 인증, Supabase admin client를 그대로 가져다 쓴다. 별도 로그인이나 팀 데이터 사본은 만들지 않는다.

## 10. 주요 예외 처리

- 팀 미선택 상태에서 점수 버튼 클릭: 버튼 비활성화 및 `먼저 팀을 선택하세요` 안내.
- 같은 버튼 빠른 연속 클릭: 첫 요청 동안 비활성화하고 서버 이벤트 ID로 중복 방지.
- 마지막 점수 취소: 가장 최근의 미취소 이벤트만 반대 점수로 기록. 원본 행은 삭제하지 않는다.
- 점수가 0 미만이 되는 취소: 서버에서 거부.
- 이미 공개된 글자: 성공으로 간주하되 DB 변경 및 중복 애니메이션 없음.
- 전체 공개 뒤 글자 클릭: 동작 없음.
- 네트워크 오류: 로컬에서 점수를 먼저 확정하지 않고 오류 알림과 재시도 제공.
- 새로고침: Supabase 상태를 다시 읽어 동일한 라운드와 점수로 복구.
- 두 관리자 화면 동시 사용: 버전 충돌을 알리고 최신 상태 새로고침 유도. 행사 중에는 한 기기 조작을 권장.

## 11. 접근성과 현장 사용성

- 글자 칸은 실제 `<button>`으로 만들고 공개 전에는 스크린 리더에 정답 글자가 노출되지 않게 한다.
- 색상만으로 팀 선택을 표현하지 않고 테두리, 체크 표시, 팀 이름을 함께 쓴다.
- 프로젝터를 고려해 본문 글자는 최소 48px, 점수는 최소 32px을 목표로 한다.
- 모든 핵심 조작은 마우스와 키보드 양쪽에서 가능하게 한다.
- 모바일 대응은 하되 실제 운영 기준 화면은 노트북/프로젝터 가로 화면이다.
- 브라우저 전체 화면(F11)에서도 하단 채점 바가 잘리지 않게 안전 여백을 둔다.

## 12. Vercel 배포 순서

1. Supabase SQL Editor 또는 CLI로 `0003_game2.sql`을 적용한다.
2. 기존 Vercel 프로젝트의 환경 변수는 그대로 사용한다. 새 비밀 키는 필요 없다.
3. 코드 배포 후 `/admin` 로그인과 `/admin/game2` 접근을 확인한다.
4. 실제 행사 기기에서 새 게임 시작, 글자 공개, +1, +3, 실행 취소, 새로고침 복구를 리허설한다.
5. 행사 시작 직전에 게임을 초기화하고 관리자 화면 하나만 연다.

## 13. 검수 기준

- 처음 진입했을 때 모든 한글 글자가 가려져 있고 띄어쓰기와 문장부호만 보인다.
- 팀 선택 없이 글자 칸을 눌러 힌트를 공개할 수 있고 점수는 변하지 않는다.
- 팀 선택 후 +1/+3을 눌렀을 때 정확한 팀 점수만 변한다.
- 전체 문장 공개만으로는 점수가 변하지 않는다.
- 마지막 점수 취소가 정확히 한 건만 되돌린다.
- 다음/이전 라운드 이동 후 공개 상태가 보존된다.
- 새로고침 후 라운드, 공개 글자, 점수가 모두 복구된다.
- 관리자 로그인이 없으면 게임 화면과 API에 접근할 수 없다.
- 12개 문장의 순서와 출처가 최종 확정본과 일치한다.

## 14. 구현 우선순위

1. 문장 및 출처 확정
2. Supabase 마이그레이션과 점수 RPC
3. 게임 보드와 개별/전체 공개
4. 점수판, +1/+3, 실행 취소
5. 라운드 이동과 종료 화면
6. 새로고침 복구, 오류 처리, 단축키
7. 프로젝터 환경 리허설 및 Vercel 배포
